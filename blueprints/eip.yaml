tosca_definitions_version: cloudify_dsl_1_3

imports:
  - http://www.getcloudify.org/spec/cloudify/4.3/types.yaml
  - plugin.yaml

node_templates:

  webservers.yaml:
    type: cloudify.nodes.File
    properties:
      resource_config:
        resource_path:
    relationships:
      - type: cloudify.relationships.contained_in
        target: elastic_ip_example

  elastic_ip_example:
    type: cloudify.nodes.Tier
